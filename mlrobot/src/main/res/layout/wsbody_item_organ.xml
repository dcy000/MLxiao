<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <CheckedTextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/wsbody_selector_item_organ_bg"
        android:gravity="center"
        android:textAlignment="center"
        android:padding="10dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="标题"
        android:textStyle="bold"
        android:textSize="@dimen/ws_font_large"
        android:textColor="@color/ws_selector_white_to_black" />

    <CheckedTextView
        android:id="@+id/ctvChoose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:visibility="gone"
        android:layout_marginRight="12dp"/>

    <ImageView
        android:id="@+id/ivFlag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignBottom="@id/tvTitle"
        android:src="@mipmap/wsbody_triangle_gray"
        android:visibility="gone"/>

    <View
        android:id="@+id/vLine"
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/ws_white"
        android:layout_alignBottom="@id/tvTitle"
        android:visibility="gone"/>

</RelativeLayout>
