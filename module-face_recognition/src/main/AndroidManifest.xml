<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gcml.module_face_recognition">
    <!-- 相机 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <!--网络-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读取手机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<meta-data-->
            <!--android:name="com.gcml.module_face_recognition.lifecycle.FaceRecognitionLifecycleCallbacks"-->
            <!--android:value="AppLifecycleCallbacks" />-->

        <activity
            android:name=".RegisterHead2XunfeiActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".FaceRecognitionActivity"
            android:screenOrientation="landscape" />

    </application>

</manifest>