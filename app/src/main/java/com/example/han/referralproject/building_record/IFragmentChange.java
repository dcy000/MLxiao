package com.example.han.referralproject.building_record;

import android.support.v4.app.Fragment;

public interface IFragmentChange {
    void lastStep(Fragment fragment);
    void nextStep(Fragment fragment);
}
